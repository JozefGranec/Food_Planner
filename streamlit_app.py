# streamlit_app.py (temporary)
import streamlit as st
st.set_page_config(page_title="Food Planner", page_icon="🍽️")
st.success("✅ App reached Streamlit and rendered UI.")
st.write("If you see this immediately, your original app has a blocking step at import-time.")
